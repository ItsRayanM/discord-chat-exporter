/** Exporter version; used in transcript meta and package tooling. */
export const EXPORTER_VERSION = "1.0.0";

/** Default subdirectory under output.dir for incremental state when stateFile is not set. */
export const INCREMENTAL_STATE_DIR = ".dcexport";

/** Prefix for generated export IDs. */
export const EXPORT_ID_PREFIX = "exp-";
